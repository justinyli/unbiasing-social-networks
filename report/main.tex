\documentclass[11pt,a4paper,twocolumn,twoside]{rho-class/rho}
\usepackage[english]{babel}
\usepackage{todonotes}

%----------------------------------------------------------
% Title
%----------------------------------------------------------

\doctype{Stat 175 Final Project Report}
\title{Debiasing Estimates for RDS in Practice}

%----------------------------------------------------------
% Authors, Affiliations and dates
%----------------------------------------------------------

\author[1]{
    Justin Li,
    Julia Poulson,
    Andrew Kang,
    Zakiriya Gladney,
    Bobby Cupps
}

%----------------------------------------------------------

\dates{May 5, 2026}

%----------------------------------------------------------
% Abstract and Keywords
%----------------------------------------------------------

\begin{abstract}
    Rho-class was designed for creating professional \LaTeX{} documents, including research articles, technical reports, and academic documentation. It enhances the writing experience through intuitive custom environments, multilingual support, and refined typography. The class integrates Fira Sans for headings and highlighted elements, Fira Mono for code listings, and STIX2 for body text, ensuring consistent styling across tables, figures, equations, listings, and captions. It is worth to mention that this template is inspired by an earlier work, the \href{https://es.overleaf.com/latex/templates/tau-class-lab-report-template/chhshmhxstsq}{tau-class}. \textit{New or updated features are marked with an asterisk (*)}.
\end{abstract}

%----------------------------------------------------------


\begin{document}

    \maketitle
    \thispagestyle{firststyle}

%----------------------------------------------------------

\section{Introduction and Motivation}

Surveys of "hidden" or "hard-to-reach" populations face obstacles as these populations of interest, which often face stigma, marginalization, or are otherwise rare, lack manageable sampling frames where the size and boundaries of the population are unknown, their members are too sparsely distributed in the general population to be reached efficiently through conventional survey design, and there exist privacy concerns that may result in refusal to participate or unreliable answers. Drug injectors in Connecticut constitute one such population. Introduced by Heckathorn (1997) to study this population's characteristics, Respondent-Driven Sampling (RDS) and its extensions by Salganik and Heckathorn (2004), Heckathorn (2002, 2007), and Volz and Heckathorn (2008), address these obstacles, as Gile and Handcock (2010) frame, through innovating in two areas: \textbf{survey design} and \textbf{inference}. 

\subsection{Survey Design}
The first innovation of RDS is its survey design, whose affordances mitigate some of the challenges of studying hard to reach populations. RDS employs a chain-referral design by beginning with a convenience sample of "seed" respondents in the target population; each participant is given a small, fixed number of coupons to distribute to peers also within the target population, who in turn recruit further participants, circumventing the absence of a sampling frame by replacing it with the social network of the population itself. Rather than searching for rare individuals through a broad population frame, RDS enables researchers to leverage the existing social ties of population members who would be prohibitively difficult to locate otherwise. Moreover, RDS' coupon mechanism removes the need for researchers to directly identify or contact members of a stigmatized population, reducing the confidentiality risks that typically depress participation by operationalizing the social trust that exists within the target population. 

\subsection{Inference}
The second innovation of RDS is in conceptualizing the waves of recruitment as a Markov chain where where each participant recruits the next participant from their network at random. It follows from this assumption that sufficient waves of recruitment eventually reduce bias from the convenience sample of seeds; the chain loses memory of where it started such that the composition of the sample is governed by the stationary distribution, rather than by the choice of seeds. This strong assumption allows researchers to reason that, adjusting for degree within the target population network, an RDS sample that underwent sufficient waves of recruitment is unbiased.

\subsection{Existing Estimators}
RDS thus allows researchers to estimate population-level characteristics, where inclusion probabilities can approximated by $\tilde{\pi}_i \propto d_i$, and population-level characteristics can then be estimated by reweighting observations by $1/d_i$. Common to these RDS approaches, however, is that non-random network structure biases sampling. 

The standard Volz-Heckathorn (VH) (2008) estimator, which models the recruitment process as a random walk, estimates the population mean of a function $g(Y)$:

\[
\hat \mu_{VH} = \frac{\sum_{t = 1}^n \frac{g(Y_{i_t})}{d_{i_t}}}{\sum_{t = 1}^n \frac{1}{d_{i_t}}}.
\]

The VH estimator rests on several key assumptions: 
\begin{enumerate}[label=\roman*)]
    \item The recruitment chain must be long enough for the Markov chain to mix, i.e., the chain must run for more steps than the mixing time of the underlying network, so that the final distribution is approximately stationary and independent of the initial seeds, though this is hardly met in-practice.
    \item Respondents must recruit uniformly at random from their network contacts, though in practice, homophily causes respondents to recruit disproportionately from like respondents, so that the recruitment chain behaves more like a walk confined to community subgraphs, with rare cross-community transitions.
    \item Self-reported degree must accurately reflect true network degree, since $\hat{y}$ weights each observation by $1/d(i)$, so underreporting inflates weights while overreporting deflates them, introducing bias in the direction of correlation between misreporting with outcome $y$.
    \item The underlying network must be connected and undirected.
    \item The random walk modeling lastly assumes sampling with replacement, though we can safely sample without replacement if the sampling fraction is very small.
\end{enumerate}

Failure to meet any of these assumptions contributes to bias into the final estimates. Assumptions (i) and (ii) contribute severely to bias in the presence of community bottlenecks, as homophilic recruitment keeps the walk within communities, and low conductance means that even a nominally long chain fails to cross community boundaries with sufficient frequency to wash out seed dependence. \textbf{\color{red} Should we talk about SH estimator here too?} This motivates the block transition probability framework of Zhang et al. (2018) and their post-stratified (PS) estimator, which models inter-community recruitment rates and forms the basis for the bias-correction method we develop here. When the graph exhibits low conductance -- when communities are connected by relatively few cross-community edges -- the mixing time of the random walk grows large, so short recruitment chains remain concentrated near their starting seeds. This problem may be particularly pronounced in populations with natural community divisions, such as "concentration" and "house" in student population, where social ties are dense within communities and sparse across them, exacerbating the "echo-chamber" effect where participants with similar covariates share the same opinion and will send the survey to friends with similar covariates.

We are interested in investigating specifically how assumption ii, uneven recruitment under homophily, holds up in practice, and how it might be addressed if unmet. In this paper, we ask: \textbf{which assumptions of RDS are met and violated in practice, and how well do existing and novel methods for adjusting bias account for these real-world results?} \textbf{\color{red} Lets come back to this at the end and make sure this is the precise question we want to ask.} Indeed, how might we de-bias our sample in situations of non-mixing? In the process of accounting for homophily within communities, we extend the VH estimator to develop a novel re-weighting mechanism. 

\section{Our Experiment}
Artificial Intelligence (AI) has been a massively transformative and fast-moving technology since the initial release of ChatGPT in 2022. A recent Crimson study found that on average, "students say they use artificial intelligence to complete 34.5 percent of their homework" (Crimson). Given this rapid rollout and adoption of AI, the question of whether or not this technology should exist or be embraced at all are overshadowed by the preeminence of technological accelerationism. Thus, we believe it is interesting and important to give voice to those who oppose AI in order to illuminate alternative viewpoints. Throughout the following, we define \textit{anti-AI} as a self-defined identity against \textit{not anti-AI}, requiring in-network participants to "opt-in" to the label.

There are several challenges to studying Anti-AI Harvard students as a target population in practice. For one, Anti-AI students are a small population -- In the Crimson's AI study, they found only around 6\% of their 300 students surveyed do not use generative AI services. Thus, we can extrapolate that the proportion of Harvard students who are Anti-AI is relatively small, and therefore collecting data on these students from a random sample would be extremely difficult. Moreover, the identity of Anti-AI is stigmatized, especially in particular academic contexts where AI has been adopted as a new norm and reflected in pejoratives like "Luddite." We also assume this population will be practically hard to reach because they're more likely to be broadly anti-technology and less trusting of institutions and data collection. Lastly, we assume that Anti-AI Harvard students exist within a relatively well-connected social network.

All of these assumptions make Anti-AI Harvard students an appropriate target population to study with RDS: Though they are difficult to study via random sampling, due to small population size and identity stigmatization, their social networks should allow us to utilize referral sampling in order to reach a sufficient number of students to collect and estimate on meaningful data.

Thus, in order to explore our research question about RDS in practice, we utilized RDS to sample anti-AI Harvard undergraduates and better understand these undergraduates' perspectives on \textbf{why} they are anti-AI, building a graph model based on the data collected.

If this were a social science paper, why these anti-AI Harvard students are anti-AI would constitute our primary research question. However, while we hope to provide some preliminary insights to answering this question through the building of our model, our focused in this paper is understanding which methodological assumptions can be met in practical applications of RDS and how we might address the violation of our assumptions, particularly that of (i) mixing and (ii) uniform recruitment.

% \section{Existing Literature}
% Literature on RDS acknowledges both its usefulness in accessing hard to reach populations and three fundamental difficulties:
% \begin{enumerate}
%     \item Over sampling of high degree nodes
%     \item Nonrandom or partial mixing
%     \item Seed dependence
%     \item Variance in tree-like structure regime 
% \end{enumerate}
% pull from the PS citations

% Volz-Heckathorn

% Post stratification

% Draw from slideshow, shows three problems with VH estimator and potential solutions

% After enough waves we get past it.
% post-stratification approach


\section{Survey Design and Data Processing}

Using RDS, we attempted to ask why anti-AI undergraduates were anti-AI. We selected 13 seed nodes and sent them our anti-AI survey. Each respondent was invited to send the survey to up to three referrals. We attempted to mitigate seed bias by choosing seeds across houses, concentrations, and class years. \textbf{\color {red}MAYBE WE CAN INCLUDE APPENDIX NOTE HERE FOR SEED NODES?} The survey itself consists of four sections: eligibility, anti-AI rationale, demographics, degree estimation, and referral. 

\subsection{Eligibility} All participants were asked if they identified as anti-AI; an affirmative response was required to proceed through the rest of the survey. 5 of 46 participants (10.9\%) did not identify as anti-AI. While they were included in our dataset, their information was not used to build our model as they fell outside the population of interest. 

\subsection{Anti-AI Rationale} We asked anti-AI participants why they were anti-AI, having them select one of four choices that best reflected their opinion in creating an outcome variable of interest: \begin{enumerate}
    \item I have concerns about about its impact on the environment
    \item I don't think the technology is trustworthy and secure
    \item I have concerns about its impact on jobs and economic inequality
    \item I have concerns about its impact on interpersonal relationships.
    \end{enumerate} 
\textbf{\color{red}{We hypothesized that while housing community may have been independent from anti-AI... trying to figure out if we should talk about cond. independence here, given she doesn't seem convinced? }}  

\subsection{Demographics} To identify possible covariates, we asked each participant for their name, political affiliation, and whether they considered themselves. Using the Harvard Facebook, we also collected information on participants' gender, concentration, year, and house. While we considered allowing participants anonymity as part of RDS' confidentiality-preserving properties, we decided that based on our research question, the demographic information provided, and our desire to minimize survey length to maximize participation, it made more sense practically to ask for each participant's full name and for information not otherwise available on the database. 

These co-variates were then cleaned and grouped. Houses were grouped into the roughly equal four house neighborhoods of Quad, River West, River Central, and River East (Dudley was included in Quad). We grouped concentrations into the three categories of Arts \& Humanities, Social Sciences, and Sciences. 

Ideally, we wanted each of the formed groups to have an equal size 


data cleaning like how we grouped multiple majors into fewer categories, and why some of these groupings are valid (both from a size perspective and ideology perspective. ideally each of the formed groups should have an equal size)


\subsection{Degree Estimation}

We also collected data about the user's degree within the Anti-AI network. This is important in order to later correct for degree, as RDS is biased towards well-connected nodes who are more likely to be referred. Modeled after the paper [Cite], we asked a specific, measurable proxy question to obtain a relative measure of in-network degree: "To the best of your knowledge, what is the number of known anti-AI Harvard students you've communicated with in the last week?" (We decided that "last week" was a a manageable amount of time for participants to consider.) Nevertheless, we had to assume these participants accurately self-reported their degree. 

\subsection{Referrals}
 
Lastly, each participant was asked to send the survey to up to three participants and provide the full name of those referred. In constructing our referral question, we attempted to lower the bias arising from preferential referrals within networks by asking respondents to "Please randomly choose up to three anti-AI Harvard undergraduates you know to refer for this survey." Nevertheless, we recognize that preferential recruitment likely still occurred. Our survey incentive, which consisted of compensating the participant \$3 for each of their referrals who successfully completed the survey also likely resulted in bias from preferential referrals, as respondents would be incentivized to send the survey to people who were more likely to fill out the survey, i.e., with whom they were closer. We decided, nevertheless, that this was necessary to maximize the number of survey responses, part of the project we struggled with. 

\section{Methodology}
\subsection{Intuition}
We propose a community-aware weighting scheme that generalizes the Volz-Heckathorn estimator by downweighting consecutive same-community referrals, which is motivated by homophily in RDS chains causing referrals to have similar responses, especially when the referred respondent is in the same community as the referrer. \textbf{\color{red}we can include that we have very limited waves here, too?}
Under strong homophily, the RDS process no longer behaves like a random walk that mixes between communities. Furthermore, opinions are often strongly correlated with community. Therefore, consecutive samples within the same community may carry less information about the overall population distribution, and cross-community referrals act as mixing events that carry disproportionately more information about population level distributions. 

Our idea is to explicitly use this structure in a weighting scheme to reduce the contribution of same-community transitions. Intuitively, this will reduce the effect of oversampling an ``echo chamber" where a group of friends with similar covariates share the same opinion and all respond to the survey and then send the survey to more of their friends with similar covariates. While the standard Volz-Heckathorn estimator accounts for bias caused by degree with the assumption of approximate mixing, it doesn't account for the community structure stopping mixing. This method is meant to improve estimation by reducing the contribution of redundant observations that could inflate variance.
\subsection{Proposed Estimator} \label{subsec:proposed_estimator}
Formally, we consider a population represented by a network $G = (V, E),$ partitioned into $K$ communities $\mathcal{C} = \{1, 2, \ldots, K\}.$ Each individual $i \in V$ has both a community label $c(i) \in \mathcal{C}$ as well as exactly one of $I$ opinions, $Y_i \in \{1, 2, \ldots I\}.$ 
Let the RDS process be defined as a sequence of referrals $\{i_t\}_{t = 1}^n,$ so the total sample size is $n.$
The standard Volz-Heckathorn estimator estimates the population mean of a function $g(Y)$ as
% TODO delete if already introduced}

\[
\hat \mu_{VH} = \frac{\sum_{t = 1}^n \frac{g(Y_{i_t})}{d_{i_t}}}{\sum_{t = 1}^n \frac{1}{d_{i_t}}}.
\]

We propose to downweight within-community referrals; that is,

\begin{equation}\label{eq:main_estimator}
w_t(\alpha) = \frac{1}{d_{i_t}} \cdot \alpha^{I(c(i_t) = c(i_{t - 1}))}
\end{equation}

\noindent where $\alpha \in [0, 1].$ Note that seeds as well as first entries into a community are not downweighted, but all following referrals that remain within the same community are downweighted. Then the modified estimator is

\[
\hat \mu(\alpha) = \frac{\sum_{t = 1}^n w_t(\alpha) g(Y_{i_t})}{\sum_{t = 1}^n w_t(\alpha)}.
\]

\noindent Note that letting $\alpha = 1$ recovers the standard Volz-Heckathorn estimator.

The optimal $\alpha$ should then be determined by community dependence; that is, the correlation of opinions within the same community and the homophily in the network. With high correlation and high homophily, referrals within the same community are likelier to lead to redundant information, meaning that $\alpha$ should be smaller. When correlation and homophily are low, then within-community samples still remain informative, so $\alpha \approx 1.$

We now formally derive an estimate for $\alpha.$ Suppose that for each individual $i,$ we have

\[
g(Y_i) = \mu + \gamma_{c(i)} + \epsilon_i
\]

% TODO reword below
\noindent with $\mu$ representing a global {\color{red} opinion}, $\gamma_{c(i)}$ representing the community specific opinion, and $\epsilon_i$ a user-level noise term. Let $E[\gamma_{c(i)}] = 0$ and $E[\epsilon_i] = 0,$ and also let $\text{Var}(g(Y_i)) = \sigma^2_\gamma + \sigma^2_\epsilon.$ Define the intra-community correlation as $\rho := \frac{\sigma^2_\gamma}{\sigma^2_\gamma + \sigma^2_\epsilon};$ it represents the proportion of variance explained by community-level opinions.
Then for two nodes $i, j,$

\[
\text{Cov}(g(Y_i, Y_j)) = \begin{cases}
    \sigma^2_\gamma, &c(i) = c(j) \\
    0, &\text{otherwise.}
\end{cases}
\]

\noindent Suppose we have $k$ consecutive samples all from the same community. Let $\bar g_k = \frac{1}{k} \sum_{t = 1}^k g(Y_{i_t}).$ This has variance

\[
\text{Var}(\bar g_k) = \frac{1}{k^2} \sum_{t = 1}^k \sum_{s = 1}^k \text{Cov}(g(Y_{i_t}, Y_{i_s})).
\]

\noindent Then using the variance and covariance from above, we have that

\begin{align*}
\text{Var}(\bar g_k) &= \frac{1}{k^2}[k(\sigma^2_\gamma + \sigma^2_\epsilon) + k(k - 1)\sigma^2_\gamma] \\
&= \frac{\sigma^2_\gamma + \sigma^2_\epsilon}{k} (1 + (k - 1)\rho). \\
\end{align*}

We define the effective sample size $n_{eff}$ as the number of independent samples that would give the same variance as the correlated sample mean $\text{Var}(\bar g_k)$; therefore, we solve

\begin{align*}
    \frac{\sigma^2_\gamma + \sigma^2_\epsilon}{k} (1 &+ (k - 1)\rho) = \frac{\sigma^2_\gamma + \sigma^2_\epsilon}{n_{eff}} \\
    \implies n_{eff} &= \frac{k}{1 + (k - 1)\rho}.\\
\end{align*}


\noindent Ideally, the total contribution of a run of $k$ samples in the same community should have weight similar to its effective sample size $n_{eff}.$ Because each observation is assigned weight $\alpha$ (besides the first observation, but we ignore this), then the total weight of the run is $k \alpha.$ Then solving

\begin{align*}
    k\alpha &\approx \frac{k}{1 + (k - 1)\rho} \\
    \implies \alpha^* &\approx \frac{1}{1 + (k - 1)\rho}.\\
\end{align*}

\textbf{\color{red} Can add the MSE thing here. With bias-variance tradeoff.}
This value of $\alpha^*$ makes sense in limiting cases: when $\rho = 0,$ all samples are important because intra-community referrals are as informative as any referral; this gives $\alpha^* = 1$ as expected. When $k = 1,$ we have $\alpha^* = 1$ which again makes sense because the original entry into a community shouldn't be downweighted. Finally, when $\rho, k$ are both large, then $\alpha^* \to 0$ which is logical because with high correlation and high homophily, new referrals within the same community are less informative and should be discounted at a higher rate.

The above derivation is dependent on assumptions like communities being observed, correlation primarily being driven by community (which appears in the formulation for $g(Y_i)$), and the sampling structure being well-approximated by runs of consecutive samples in a single community of typical length $k.$

\section{Empirical Testing}
\subsection{Experimental Setup}
We use simulated networks generated from stochastic block models (SBMs) to evaluate the performance of our weighting estimator. We do this because we have control over levels of homophily and correlation of opinion with community, and we can also generate the ground-truth community and opinion labels. 

We first consider an SBM with 7 blocks and 120 total nodes. To introduce realistic heterogeneity in community sizes, we do not make equal block proportions, but assign nodes to communities by sampling a probability vector from a Dirichlet distribution and assigning nodes proportionally to the sampled probability vector. Edges are then generated according to the SBM with intracommunity edge probability $p_{in} = 0.5$ and intercommunity edge probability $p_{out} = 0.02,$ giving a strongly associative graph. There are four opinions (encoded 0-3); each community is assigned a designated dominant opinion, sampled uniformly from the four opinions. Each node in the community's opinion is the same as the community dominant opinion with probability 90\%, and it is any of the other three opinions with equal probability (3.3\%). 

Let us consider the value of $\alpha^*$ derived from the above specifications. We chose to have a 90\% correlation of individual opinion with community dominant opinion, so we have $\rho = 0.9.$ Furthermore, we let $k$ be the expected length of consecutive samples in a single community. We estimate the referral process to be such that each person refers exactly one other person. Then we estimate the probability that the referred person is in the same community by considering a node's expected number of neighbors in the same community divided by that node's total expected neighbors. If node $i_t$ is in community $m$ with $N$ total nodes and $n_c$ nodes in community $c,$ then

\[
P(c(i_t) = c(i_{t - 1})) \approx \frac{n_m p_{in}}{n_m p_{in} + (N-n_m) p_{out}}.
\]
\\

\noindent For simplicity's sake, we estimate that $n_m p_{in} + (N - n_m) p_{out} \approx n_k.$ This is because we are applying this method in a regime where we have high community cohesion ($p_{in} \to 1$) and low inter-community connection ($p_{out} \to 0$), meaning that the first term dominates, and we have the $n_m$ approximation. Therefore since $P(c(i_t) = c(i_{t - 1})) \approx p_{in},$ the length of a consecutive run within the same community follows a first success distribution with parameter $1 - {p_in}$ so

\[
k\approx \frac{1}{1 - p_{in}}
\]

\noindent which gives

\[
\alpha^* = \frac{1}{1 + (k - 1)\rho} = \frac{1}{1 + \frac{p_{in}}{1 - p_{in}}\rho}.
\]

\noindent Here, because $p_{in} = 0.5$ and $\rho = 0.9,$ we have that $\alpha^* \approx 0.526.$

We simulate RDS on each generated network using 2 initial seed nodes; each respondent then recruits 2 neighbors, and sampling continues until a total sample size of 30 nodes is reached. This setup creates a controlled environment with strong correlation of community with opinion as well as high homophily. 

Because the entire population's distribution is known, we can use Kullback-Leibler (KL) divergence to measure discrepancy between true population distribution and the distribution recovered by each estimator. Let $P$ denote the true distribution of opinions in the full SBM population, and $\hat P$ denote an estimated distribution. Then

\[
D_{KL}(P \| \hat P) = \sum_x P(x) \log \left(\frac{P(x)}{\hat P(x)}\right).
\]

KL divergence is useful because we're interested in comparing full categorical distributions, and we want to penalize misallocation in any category. Furthermore, the asymmetry in KL divergence is helpful here because if certain groups with less popular opinions are systematically undersampled (making $\hat P(x)$ smaller), then KL divergence will place a larger penalty compared to overestimating those same groups. This is especially relevant in the RDS setting where we aim to have a fair representation of the population's true opinion distribution.

\subsection{Experimental Results}
We first see a single result of the SBM graph generation and estimator correction. The table below compares the KL divergence between each quantity and the true population distribution, and a curve comparing KL divergence to different values of $\alpha$ is shown in Fig \ref{fig:single_trial}. 

\begin{center}
\begin{tabular}{lr}
\toprule
& KL Divergence \\
\midrule
Sample Frequency vs.~Population & 0.287278 \\
VH vs.~Population & 0.132597 \\
Adjusted VH vs.~Population & 0.053308 \\
\bottomrule
\end{tabular}
\end{center}

Unsurprisingly, the raw sampled frequencies show the largest deviation from true population distribution. The Volz-Heckathorn estimate reduces error by accounting for degree-related sampling bias, but still performs worse than the adjusted estimator that we proposed. This single-trial result is consistent with our hypothesis that explicitly accounting for community-related correlation can reduce the distributional bias more than the vanilla Volz-Heckathorn estimator. 

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{img/single_trial_alpha_vs_kl.pdf}
    \caption{Setting $\alpha = \alpha^*$ reduces KL divergence compared to $\alpha = 1$, the vanilla Volz-Heckathorn estimator.}
    \label{fig:single_trial}
8\end{figure}

We now consider a larger experiment of 100 samples; each sample is generated independently with the same SBM generation and RDS simulation as in the previous part. In each trial, we compare the KL divergence of the estimator at $\alpha = 1$ (no adjustment to Volz-Heckathorn) with the KL divergence at $\alpha = \alpha^*,$ with $\alpha^*$ as found by our heuristic in \ref{subsec:proposed_estimator}. Specifically, we compute the difference

\[
D_{KL}(P \| \hat P_{\alpha = 1}) - D_{KL}(P \| \hat P_{\alpha^*})
\]

\noindent so a positive value means that an improvement has occurred using $\alpha^*,$ and larger values mean greater reduction in distributional error achieved by the proposed method. 

The resulting histogram is shown in Fig \ref{fig:kl_histogram}. Across the 100 trials, we see that the proposed method yields a positive improvement in KL divergence in 68\% of cases. The mean difference in KL divergence is 0.0198, indicating that on average, modifying $\alpha$ with our heuristic reduces distributional error. Both the moderately high success rate and positive mean improvement are promising, and the adjustment seems capable of reduce the underlying bias in the sampling process with some consistency. Still though, the gains are not uniformly large, and 32\% of trials exhibit worsened performance using $\alpha^*.$ While the heuristic may be beneficial often, it is not universally optimal and seems quite sensitive to the specific realization of both the network and the sampling process. 

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{img/kl_difference_histogram.pdf}
    \caption{Distribution of KL divergence differences across 100 trials, where positive values indicate improvement from the proposed adjustment.}
    \label{fig:kl_histogram}
\end{figure}

\textbf{\color{red} can make a graph of KL divergence difference over different correlational values}

\textbf{\color{red} talk about unrealistic thigns that we did. eg everyone referred 2 nodes (until the end was reached). maybe $\alpha$ should depend on seed nodes and size of samples.}

\section{Application on Data}
\subsection{Summary of Data}
We collected 41 samples of individuals with anti-AI sentiment. 
First, we see in Fig \ref{fig:anti_ai} that anti-AI sentiment is primarily for economic, interpersonal, and environmental reasons, with trust and security being a minor reason. 

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{img/anti_ai_reasons_pie_chart.pdf}
    \caption{Distribution of reasons for being anti-AI.}
    \label{fig:anti_ai}
\end{figure}

Next, we see distributions across covariates like year, gender, and house in Fig \ref{fig:covar_distr}. Upperclassmen are more represented in the sample, which is likely due to seed selection effects (we knew more upperclassmen who were anti-AI, so more upperclassmen served as seeds) and propagation through the referral process. Additionally, the sample contains a higher proportion of women relative to other gender categories. In contrast, the distribution of responses across houses is relatively even.

\begin{figure}
    \centering
    \includegraphics[width=0.9\linewidth]{img/survey_demographics.pdf}
    \caption{Distribution of respondents across year, gender, and house.}
    \label{fig:covar_distr}
\end{figure}

Finally, we see a distribution of respondents over concentration grouped by field in Fig \ref{fig:grouped_concentration}. We group the concentrations according to Harvard's own designation \cite{Harvard_College}. The resulting distribution is relatively even across the three major groups, which is helpful because it will ensure no single academic division disproportionately drives the composition of the sample, and it also means that some amount of mixing has already occurred in the RDS survey run. 

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{img/grouped_concentration_distribution.pdf}
    \caption{Concentration grouped by field.}
    \label{fig:grouped_concentration}
\end{figure}

Finally, we see the resulting network generated by RDS, clustered and colored by wave at which each individual was recruited in \ref{fig:RDS_network}. Clearly, sampling process does not form a fully expanding referral tree as we hoped. Many participants (including some seeds) do not refer any additional individuals, meaning making recruitment chains terminate early. As a result, the realized sample likely has uneven coverage of the underlying population. 

\begin{figure}
    \centering
    \includegraphics[width=\linewidth]{img/RDS_network.pdf}
    \caption{Visualization of the realized RDS network.}
    \label{fig:RDS_network}
\end{figure}

\subsection{Application of Proposed Methodology}
To apply our methodology, we must find the appropriate reweighting constant $\alpha.$ To do so, we will use the formula for $\alpha^*$ which requires that we have estimates for $p_{in}$ and $\rho.$ We will model the network as an SBM, so we first construct community structure by using the grouped concentrations. These community assignments can then be used to estimate homophily. We must also estimate the correlation of community opinion with the dominant opinion of that community. 




% TODO make a table of distributions using each estimator style



give an algorithm for this (eg estimate $\rho, k$ and then get $\alpha$. then run on data)
    Tbd...

\textbf{\color{red} Issues with data: people can be part of multiple concentrations}
		
Once we reweight the data by alpha, we get a a distribution that we believe more closely
estimates the true distribution of responses among our community in question (Luddites at Harvard). By the nature of our undertaking, we do not know the true distribution, but do have knowledge about the covariate pool that should be represented in the true distribtution of luddites, namely that house affiliation is independent of response and of being a luddite, so we expect a uniform distribution of houses through each bucket of the survey. 

We compare our reweighted covariate pool with the unweighted pool and do indeed that we get closer to uniform


? But not close enough and therefore we conclude that our data does not fit our assumptions well enough to be applicable.

\section{Future Directions}

want some more formality on $\alpha.$ Test on more values of $p, q;$ see when this works and doesn't work. 
	

\printbibliography

%----------------------------------------------------------

\end{document}